---
"@scheman/core": major
---

Preserve lazy pipeline metadata and defaults across supported Zod versions, and narrow the optional Zod peer contract to the tested ranges `>=3.24.0 <4` and `>=4.0.0 <5`.
